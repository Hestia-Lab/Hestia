/**
 * @hestia/common — shared cryptographic foundations (SPEC §3).
 *
 * The spine of the protocol: encoders here are byte-identical to what the circuits
 * constrain and the contracts recompute.
 */
export {};
